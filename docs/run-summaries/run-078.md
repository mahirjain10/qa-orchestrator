# Run Summary — 078

## Run
- ID: 078
- Date: 2026-05-22
- Agent: opencode

## Goal
Implement Phase 5 of the Agentic Recovery plan to break the infinite `observe_ui` loop when encountering a 404 page and ensure the 404 warning is visible in the trace logs.

## What happened
Analyzed the user-provided trace logs from Run 077 and confirmed that while 404 detection and Recovery steering were working, the LLM was trapped by a conflicting prompt rule (Rule 2) that forced it to call `observe_ui()` repeatedly after any failure state. Furthermore, the trace logs failed to show the 404 warning because `buildObservationSummary` dropped it.

1.  **Modified `packages/llm/prompts.go`**: Updated `SystemPromptTemplate` to explicitly exclude 404s from Rule 2, instructing the LLM to skip the loop and proceed directly to `navigate(root_domain)`.
2.  **Modified `packages/agents/engine/engine.go`**: Updated `buildObservationSummary` to check for and append the `"warning"` string to the summary output, ensuring TUI operators can see 404 detections in the Trace view.

## Files modified
- `packages/llm/prompts.go`
- `packages/agents/engine/engine.go`
- `docs/CURRENT.md`
- `docs/run-summaries/run-078.md` (this file)

## Tests
- `go build ./...` — Clean. No syntax issues.
- User will run the TUI to perform E2E verification of the loop break.

## Status
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
Nothing went wrong during this phase.

## What to watch for
The other bug findings from the user's audit report (e.g., `autoObserve` executing `observe_ui` twice, trace store rewriting the entire file, etc.) are still outstanding. We verified they exist but have not fixed them yet, pending further instruction.