# Run Summary — 103

## Run
- ID: 103
- Date: 2026-05-25
- Agent: opencode

## Goal
Fix 2 bugs from bug-report-sunday.md: Engine BUG 5 (pause-before-steering drain order in engine.go) and config.go envAllowFallbacks alignment.

## What happened
1. **Engine BUG 5 — Pause-before-steering drain order:** In `runAutonomousFlow` and `runGuidedFlow` in `packages/agents/engine/engine.go`, `drainSteeringEvents` was called AFTER `handlePauseResume`. This meant if a user submitted a skip/retry steering command, it wouldn't be processed until after a pause/resume cycle completed. Fixed by moving `drainSteeringEvents` before `handlePauseResume` in both loops.
2. **config.go constant alignment:** Fixed misaligned `envAllowFallbacks` constant at line 19 of `packages/llm/config.go` — was missing tabs for alignment with surrounding constants.

## Files modified
| File | Bug |
|------|-----|
| `packages/agents/engine/engine.go` | Bug 5 — reordered drainSteeringEvents before handlePauseResume |
| `packages/agents/engine/engine_test.go` | New test: `TestGuidedFlow_SteeringSkipBeforePause_ProcessedFirst` |
| `packages/llm/config.go` | envAllowFallbacks alignment |

## Tests
- All 18 package test suites PASS
- Engine package: 1 new test added — `TestGuidedFlow_SteeringSkipBeforePause_ProcessedFirst`
- Test verifies: steering skip event submitted before pause is consumed during `drainSteeringEvents`, step is skipped (not executed), flow completes with `OutcomePass`

## Status
- Tests: PASSED
- Compile: PASSED (pre-existing TUI build error in `apps/tui/internal/screens/main.go:1022` unrelated)
- Phase: COMPLETE

## What went wrong
- Initial edit had a typo (`rrunID` instead of `runID`) — caught and fixed immediately.
- First version of the test used `OutcomeSkip` expectation — but `SteerSkip` skips the current step, not the entire flow. Fixed to expect `OutcomePass` (flow completes after step is skipped). Debug logging helped trace the flow behavior.

## What to watch for
- Steering commands (retry/skip) are now processed before pause checks. If a user sends both a steering command and a pause simultaneously, the steering command takes effect first.
- No behavioral change if no steering events are pending.
- `SteerSkip` skips the current step, not the entire flow — `OutcomeSkip` only happens on cancellation.

## Key decisions
- Reordered drain before pause in both guided and autonomous loops for consistency.
- envAllowFallbacks alignment matches tab style of all other constants in the const block.
- Test uses `store.UpdateStatus(Pausing)` before `RunFlow` + goroutine for deferred `Resuming` to reliably trigger pause in the loop.

## Next
- Verify steering commands during pausing work correctly in TUI.