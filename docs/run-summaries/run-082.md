# Run Summary — 082

## Run
- ID: 082
- Date: 2026-05-22
- Agent: opencode

## Goal
Fix SauceDemo browser session isolation, improve observe_ui heading visibility, and create a verified-real-URL parallel campaign.

## What happened
1. **Session sharing (runtime.go)** — Changed `NewFlowRuntime()` to accept `...*playwright.StorageState`. Added `StorageState()` method to `FlowBrowserRuntime` so cookies+localStorage can be extracted before the context closes. The variadic signature maintains backward-compatibility with existing callers.

2. **Campaign runner (main.go)** — Added `flowStorageStates` map alongside `flowResults`. Before creating a flow's browser, checks if any completed dependency has a storage state and passes it to `NewFlowRuntime`. After each flow completes, saves its storage state for downstream dependents.

3. **Headings in observe_ui (registry.go:14)** — Added `h1, h2, h3, h4, h5, h6, [role="heading"]` to the `document.querySelectorAll()` CSS selector so titles and headings appear in the elements list visible to the LLM.

4. **New campaign YAML (campaigns/real-urls-parallel.yaml)** — 8 flows (5 independent + 3 dependent) using only verified real URLs: `practice-test-login/`, `practice-test-exceptions/`, `logged-in-successfully/`, root `/`.

5. **Makefile** — Replaced hardcoded "10" with dynamic `$(words $(CAMPAIGNS))` so it auto-updates when campaigns are added/removed.

## Files created/modified
- `packages/browser-runtime/runtime.go` — `NewFlowRuntime` accepts storage state, added `StorageState()` method
- `packages/browser-runtime/tools/registry.go` — Added headings to observe_ui CSS selector
- `apps/tui/cmd/main.go` — Storage state tracking and propagation between dependent flows
- `campaigns/real-urls-parallel.yaml` — NEW: 8-flow parallel campaign with verified real URLs
- `Makefile` — Dynamic campaign count in help text

## Tests
- 14 test suites PASS (agents/engine, agents/planner, agents/recovery, browser-runtime, browser-runtime/tools, llm, campaign parser, validator, reporting, runtime, shared/types, storage/*, apps/tui/cmd, ui components/screens/style/util)
- 0 failures

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- Nothing — all changes compiled and tested clean on first pass.

## What to watch for
- `NewFlowRuntime` now has a variadic parameter; existing callers with `NewFlowRuntime()` still work.
- `main.go` now imports `playwright` directly (needed for `*playwright.StorageState` type).
- Storage state is only shared between directly-depended flows in the same dependency chain. Parallel independent flows do not share state.

## Key decisions
- Used Playwright's native `StorageState` mechanism rather than custom cookie JSON — ensures localStorage + IndexedDB are also preserved, not just cookies.
- `NewFlowRuntime` uses variadic `...*playwright.StorageState` instead of a breaking signature change — all existing callers compile without changes.
- New YAML named `real-urls-parallel.yaml` rather than modifying `large-parallel.yaml` — preserves the original as a negative test for 404 detection.

## Next
- Re-run SauceDemo campaign to validate session sharing and heading visibility
