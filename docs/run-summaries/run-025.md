# Run Summary — 025

## Run
- ID: 025
- Date: 2026-05-19
- Agent: codex

## Goal
Fix all remaining cross-phase bugs/inconsistencies from end-to-end adversarial review (MVP + V2).

## What happened
1. Lifecycle WAITING_FOR_INPUT channel fix:
   - Initialized `inputCh` in `NewLifecycleController`.
   - Added regression test to verify `RequestInput()` emits on `InputCh()`.
2. Reporting completion timestamp correctness:
   - `GenerateCampaignSummary` now uses `Session.CompletedAt` when present, falling back to `UpdatedAt` only when needed.
   - Duration now computed from `StartedAt -> CompletedAt` summary fields.
   - Added test assertion for completed-at source behavior.
3. Natural-language parser ID hardcoding fix:
   - Replaced fixed `auto-flow-1` with generated `auto-flow-<timestamp>` ID.
   - Updated parser test to validate prefix contract instead of exact static ID.
4. Skip semantics clarification:
   - Added new flow state `SKIPPED_USER`.
   - Engine maps `OutcomeSkip` to `FlowStateSkippedUser` (instead of upstream-dependency skip bucket).
   - Session flow timestamp finalization updated to include `SKIPPED_USER`.
   - Reporting skipped-flow counts updated to include both upstream and user skip states.
5. ID generation robustness:
   - Added fallback behavior for `NewRunID` and `NewSessionID` when `crypto/rand.Read` fails.

## Files created/modified
- packages/runtime/lifecycle.go
- packages/runtime/lifecycle_test.go
- packages/reporting/reporter.go
- packages/reporting/reporter_test.go
- packages/orchestrator/campaign/parser.go
- packages/orchestrator/campaign/parser_test.go
- packages/shared/types/campaign.go
- packages/shared/types/session.go
- packages/agents/engine/engine.go
- packages/agents/engine/engine_test.go
- logs/runs/2026-05/run-025.jsonl
- docs/run-summaries/run-025.md
- docs/CURRENT.md

## Tests
- `go test ./...` passed across all packages.

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- No regressions during this run.

## What to watch for
- `SKIPPED_USER` introduces a new flow-state value; any downstream consumers with strict enums should include it.
- Natural-language parser now emits unique IDs per parse; tests relying on exact static ID must use prefix-based assertions.

## Key decisions
- Kept parser ID generation simple and deterministic enough for uniqueness without introducing additional global counters.
- Preserved backward compatibility in reporting by counting both skip variants in the same skipped aggregate metric.

## Next
- Optional polishing: display separate counters for upstream skip vs user skip in terminal summary for operator clarity.
