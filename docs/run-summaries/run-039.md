# Run Summary — 039

## Run
- ID: 039
- Date: 2026-05-19
- Agent: qwen3.6-plus-free

## Goal
Fix autonomous planner infinite loop — LLM generates redundant verification steps instead of stopping when goal is achieved.

## What happened
1. Identified root cause: engine.go:338 already handles `if planStep.Tool == "finish" { break }`, but the LLM was never told `finish` exists as an available tool.
2. Added `finish` to `getDefaultLLMTools()` in `engine.go` — covers default (mock) mode.
3. Added `finish` to browser registry in `registry.go` — covers `BROWSER_MODE=real`.
4. Updated system prompt in `prompts.go` to add constraint: "Use the 'finish' tool as soon as the goal is achieved — do not generate redundant verification steps after the goal is met."
5. All tests pass, binary builds successfully.

## Files modified
- `packages/agents/engine/engine.go` — added `finish` to `getDefaultLLMTools()`
- `packages/browser-runtime/tools/registry.go` — registered `finish` as no-op tool + description
- `packages/llm/prompts.go` — added finish instruction to system prompt constraints

## Tests
- `go test ./...` — PASSED (all 22 packages)
- `go build -o ./bin/qa-orchestrator ./apps/tui/cmd/main.go` — PASSED

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- None.

## What to watch for
- Existing test `TestAutonomousFlow_MultiTurnUntilFinish` already uses `finish` in mock LLM responses — confirms engine handling works.
- The fix is additive: adding a tool to the prompt doesn't break existing behavior.
- LLM may still occasionally be stubborn and not use finish — that's an LLM behavior issue, not a code issue.

## Key decisions
- Added `finish` as a real registered tool (no-op) rather than just a prompt hint — ensures consistency between what LLM sees and what executor can handle.
- Updated prompt constraints rather than just adding the tool — teaches LLM WHEN to use it, not just that it exists.

## Next
- Test with real autonomous campaign to verify LLM now stops after goal is achieved instead of looping 20 times.