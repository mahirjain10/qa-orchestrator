# Run Summary — 015

## Run
- ID: 015
- Date: 2026-05-19
- Agent: codex

## Goal
Fix Phase 1 parser/validator inconsistencies found in adversarial review.

## What happened
1. Reviewed current state and latest summary (`run-014`) plus architecture and validation constraints.
2. Updated `CampaignParser.validate` to call `DependencyValidator.Validate`, so both missing dependencies and circular dependencies are enforced during parse-time validation.
3. Removed duplicated dependency-existence validation from parser and centralized dependency integrity checks through `DependencyValidator`.
4. Updated `ParseNaturalLanguage` to generate schema-valid campaigns with default `version: "1.0"`, flow `mode: autonomous`, and flow `priority: medium`, then validate before return.
5. Fixed formatting/readability issue in `topologicalSort` loop in dependency validator.
6. Hardened parser tests to prevent false positives by building tests from a valid baseline campaign and asserting targeted error semantics.
7. Added parser-level circular dependency test (`TestValidate_CircularDependency`) to verify cycle rejection via parser validation path.

## Files created/modified
- packages/orchestrator/campaign/parser.go
- packages/orchestrator/validator/dependencies.go
- packages/orchestrator/campaign/parser_test.go
- logs/runs/2026-05/run-015.jsonl
- docs/run-summaries/run-015.md
- docs/CURRENT.md

## Tests
- `go test ./packages/orchestrator/campaign ./packages/orchestrator/validator` passing
- `go test ./...` passing

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE (Phase 1 bug-fix hardening)

## What went wrong
- Initial `go build ./...` failed due sandbox VCS/cache access (`error obtaining VCS status` and Go build cache access denied).
- Re-ran compile with local cache and `-buildvcs=false`; compile passed.

## What to watch for
- Parser validation now depends on `DependencyValidator` output formatting; changes in validator error formatting can affect parser-level error assertions.
- Natural-language parser now validates before returning; future changes to required schema fields must be mirrored there.

## Key decisions
- Dependency validation was centralized to avoid duplicated checks and ensure cycle detection is always applied.
- Test setup was shifted to a schema-valid base fixture to ensure each test fails for the intended rule only.

## Next
- Proceed with V2 Phase 2 (`packages/llm`) once the bug-fix baseline is approved.
