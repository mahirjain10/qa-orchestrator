# Run Summary — 004

## Run
- ID: 004
- Date: 2026-05-18
- Agent: gemini-cli

## Goal
Fix critical UI bugs and architecture duplication introduced during Phase 2.

## What happened
1. Added `tea.Tick` to trigger real-time UI polling.
2. Fixed UI Navigation Bug where the list cursor snapped to the top every second.
3. Deleted duplicate `CampaignListComponent` inside `main.go`.
4. Deleted duplicate `commands` directory.

## Files created/modified
- `apps/tui/internal/screens/main.go`
- `apps/tui/internal/components/campaign_list.go`

## Tests
- 22 tests passing

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- Live polling introduction caused a UI bug because `SetCampaigns` unconditionally reset the selected index to 0 every second.
- Duplicate component structures caused architectural confusion.

## What to watch for
- When using tick-based auto-refresh in Bubbletea, ensure UI state updates handle bounds checking carefully so user interaction (like cursor position) isn't wiped out.
- Ensure embedded structs don't shadow properly namespaced components.

## Key decisions
- Handled bounds checking dynamically in `SetCampaigns` (`if m.selected >= len(m.campaigns) { m.selected = 0 }`).
- Removed duplicated `commands` directory in favor of `screens` handlers.

## Next
- Phase 3: Agent loop implementation.