# Run Summary — 105

## Run
- ID: 105
- Date: 2026-05-25
- Agent: opencode

## Goal
Fix TUI bugs T11 and T12 from bug-report-sunday.md.

## What happened
1. **T11 — Filter textinput Enter returns stale cmd (`main.go:287-292`):** `FilterInput.Update(msg)` was called on line 287 to capture `cmd`, but then `FilterInput.SetValue("")` on line 292 mutated the model. The `cmd` returned to Bubble Tea referenced the pre-reset state. Fixed by restructuring the function to call `Update(msg)` once per branch — only after all state mutations (SetValue, FilterMode, etc.) are complete.

2. **T12 — `j`/`k` navigation and viewport scrolling out of sync (`main.go:477-478`):** `handleContentUp`/`handleContentDown` called `tracePanel.Prev()`/`Next()` to change selection but never called `UpdateViewportContent()` to regenerate the viewport content with the new cursor position. Added `UpdateViewportContent()` + `scrollTraceToSelection()` helper that adjusts `Viewport.YOffset` to keep the selected row centered in the visible area.

## Files modified
| File | Bug |
|------|-----|
| `apps/tui/internal/screens/main.go` | T11 + T12 — filter cmd staleness, viewport sync on selection change |

## Tests
- All 22 test suites PASS
- Build clean, vet clean

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- First edit of `scrollTraceToSelection` was placed in the wrong location (inside `Update` function) — the `return m, nil` + `}` pattern matched multiple locations. Fixed by removing the misplaced block and adding the function at the correct location after `handleContentDown`.

## What to watch for
- When editing Bubble Tea handlers, always capture `cmd` after all model mutations, not before.
- When changing list/trace selection, always update the viewport content and scroll to keep the selection visible.

## Key decisions
- `scrollTraceToSelection` centers the selected row in the viewport (`YOffset = selectedRow - halfViewport`), with a floor of 0.

## Next
- Fix remaining TUI bugs from bug report: T1-T3, T6-T10.