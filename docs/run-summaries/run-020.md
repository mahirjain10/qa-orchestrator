# Run Summary — 020

## Run
- ID: 020
- Date: 2026-05-19
- Agent: codex

## Goal
Finalize Phase 4 hardening and close remaining audit gaps before Phase 5.

## What happened
1. Tool registry hardening completed:
   - `Execute()` now validates incoming params against tool metadata (`Required` + type checks) before calling tool functions.
   - Added metadata presence check to prevent executing tools without docs/schema.
   - Kept read locking on map access.
2. Wait-state metadata consistency:
   - Updated `wait_for.state` metadata description to explicitly state default behavior (`default: visible`).
3. History hot-path optimization completed:
   - Added full history string cache on `Plan` (`historyCache`, `historyBuilt`, `historyDirty`).
   - `GetHistory()` now incrementally extends cached history instead of rebuilding from step 1 every turn.
   - Added `InvalidateHistoryCache()` and wired invalidation in planner `UpdatePlan()` when skip/reason mutates.
4. Tests added/updated:
   - Added metadata-validation test for registry required/type enforcement.
   - Added history cache/invalidation behavior test.

## Files created/modified
- packages/browser-runtime/tools/registry.go
- packages/browser-runtime/tools/registry_unit_test.go
- packages/agents/types/agent.go
- packages/agents/types/agent_test.go
- packages/agents/planner/planner.go
- logs/runs/2026-05/run-020.jsonl
- docs/run-summaries/run-020.md
- docs/CURRENT.md

## Tests
- `go test ./...` passed across all packages.

## Status
- Tests: PASSED
- Compile: PASSED
- Phase: COMPLETE

## What went wrong
- No functional regressions found during this run.

## What to watch for
- If any component mutates `PlanStep` fields directly (outside planner update path), it should call `InvalidateHistoryCache()` to keep history strictly fresh.
- Metadata-driven validation currently enforces parameter presence/types for declared params; unknown extra params are still allowed.

## Key decisions
- Validation in `Execute()` makes metadata the single source of truth for required/type semantics.
- History caching was implemented with explicit invalidation to balance performance and correctness.

## Next
- Start V2 Phase 5: Agent Engine V2 (Mode Routing).
